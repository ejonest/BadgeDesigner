{%- if page.handle == 'name-tag-blanks' or template.suffix == 'name-tag-blanks' -%}
<section class="aqb-bl-trust-bar">
  <div class="aqb-bl-trust-bar__inner">
    {%- for block in section.blocks -%}
      <div class="aqb-bl-trust-bar__item" {{ block.shopify_attributes }}>
        <span class="aqb-bl-trust-bar__icon" aria-hidden="true">{{ block.settings.emoji }}</span>
        <div>
          <div class="aqb-bl-trust-bar__title">{{ block.settings.title | escape }}</div>
          {%- if block.settings.subtitle != blank -%}
            <div class="aqb-bl-trust-bar__sub">{{ block.settings.subtitle | escape }}</div>
          {%- endif -%}
        </div>
      </div>
    {%- endfor -%}
  </div>
</section>
{%- endif -%}

{% schema %}
{
  "name": "AQB Blanks Trust Bar",
  "tag": "section",
  "class": "section",
  "blocks": [
    {
      "type": "item",
      "name": "Trust item",
      "settings": [
        { "type": "text", "id": "emoji", "label": "Emoji", "default": "🎨" },
        { "type": "text", "id": "title", "label": "Title", "default": "6 Colors" },
        { "type": "text", "id": "subtitle", "label": "Subtitle", "default": "White, gold, silver, black, blue, red" }
      ]
    }
  ],
  "presets": [{ "name": "AQB Blanks Trust Bar" }]
}
{% endschema %}
